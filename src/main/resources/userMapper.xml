<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nc.mapper">
    <select id="queryUserById" resultType="com.nc.model.User">
        select * from user where id = #{id}
    </select>

    <insert id="addUser" parameterType="com.nc.model.User">
        insert into user(username, address) values(#{username}, #{address})
    </insert>

    <insert id="addUser2" parameterType="com.nc.model.User">
        <selectKey resultType="java.lang.String" keyProperty="id" order="BEFORE">
          select uuid();
        </selectKey>
        insert into user(id,username,address) values (#{id},#{username},#{address})
    </insert>

    <delete id="deleteUserById" parameterType="java.lang.String">
        delete from user where id = #{id}
    </delete>

    <update id="updateUserById" parameterType="java.lang.String">
        update user set username = #{username} where id = #{id}
    </update>

    <select id="queryUser" resultType="com.nc.model.User">
        select * from user
    </select>
</mapper>
